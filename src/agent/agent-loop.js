// Anthropic tool use の手動エージェントループ(実装は後続ステップ)
export class AuthError extends Error {}

export const runAgent = async ({onAssistantText}) => {
    // TODO: Claude API 結線(スタブ)
    onAssistantText('(エージェントループは未実装です)');
};
